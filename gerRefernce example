function onLoad() {
    //Type appropriate comment here, and begin script below
    var caller = g_form.getReference('caller_id', alertCaller);

    function alertCaller(caller) {
        if (caller.vip == "true") {
            alert("the caller is vip");
        }
    }
}
